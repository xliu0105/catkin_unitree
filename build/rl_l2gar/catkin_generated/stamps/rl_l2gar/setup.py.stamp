from setuptools import setup, find_packages
# from distutils.core import setup

setup(
  name='rl_l2gar',
  version='0.0.0',  # 这个版本号必须要和package.xml中的版本号一致
  packages=find_packages(where="scripts"),  # 这里的包名必须要和src文件夹下的包名一致
  package_dir={'': 'scripts'},
  scripts=['scripts/rl_l2g/rl_gazebo.py'],  # 这里的脚本名必须要和scripts文件夹下的脚本名一致
)